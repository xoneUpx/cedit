import React from 'react';
import { NameType } from './types/types';
const App = ( {name}: NameType ) => {
  return (
    <div>
          Learn React {name}
          </div>
  );
}

export default App;
